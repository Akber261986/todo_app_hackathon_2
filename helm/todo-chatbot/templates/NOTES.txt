Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

The Todo Chatbot application has been deployed to your Kubernetes cluster.

Frontend Service: {{ include "todo-chatbot.fullname" . }}-frontend:{{ .Values.frontend.service.port }}
Backend Service: {{ include "todo-chatbot.fullname" . }}-backend:{{ .Values.backend.service.port }}

{{- if .Values.ingress.enabled }}
You can access the frontend at:
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
To access the frontend service, you can port forward to it:
  kubectl port-forward svc/{{ include "todo-chatbot.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }} -n {{ .Release.Namespace }}
{{- end }}

For the backend API:
  kubectl port-forward svc/{{ include "todo-chatbot.fullname" . }}-backend {{ .Values.backend.service.port }}:{{ .Values.backend.service.port }} -n {{ .Release.Namespace }}